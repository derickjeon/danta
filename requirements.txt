beautifulsoup4==4.13.4    # 웹 스크래핑 (네이버 증시 데이터)
feedparser==6.0.11        # 매일경제 RSS 뉴스 파싱
lxml==6.0.0               # HTML/XML 파싱 가속화
matplotlib==3.7.5         # 차트 생성
numpy==1.26.4             # 수치 계산
pandas==1.5.3             # 데이터 처리
pycoingecko==3.2.0        # 코인 가격 API
requests==2.32.4          # 웹 요청 (네이버, 환율 등)
